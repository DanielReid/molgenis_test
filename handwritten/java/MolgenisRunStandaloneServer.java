import java.io.File;

/**
 * Run MOLGENIS standalone server instead of using Tomcat
 */
public class MolgenisRunStandaloneServer
{
	public static void main(String[] args)
	{
//		MolgenisServerOptions options = new MolgenisServerOptions(new File("molgenis.properties"));
//		MolgenisServer server = new MolgenisServer(options);
//		server.start("MolgenisServlet");
	}
}
